# 학습·변환용. iOS 앱 쪽은 별도다.
#
# ultralytics 가 torch 를 끌고 온다. GPU 를 쓰려면 먼저 CUDA 빌드를 설치할 것:
#   https://pytorch.org/get-started/locally/
#
# coremltools 는 Python 3.9~3.12 를 요구한다. 이 머신은 3.9.12 로 하한이라,
# 문제가 생기면 3.11 로 올리는 것을 권한다.

ultralytics>=8.3.0
coremltools>=8.0
pyyaml>=6.0
